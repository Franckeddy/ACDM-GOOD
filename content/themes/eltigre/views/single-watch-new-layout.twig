{% extends "base.twig" %}

{% block content %}
	{% for post in posts %}
		<!-- ===== DESKTOP ===== -->
		<div class="content-wrapper content-wrapper-desktop">
			<article class="post-type-{{ post.post_type }} single__watches single__watches--new-layout desktop" id="post-{{ post.ID }}">
				<div class="single__watches--container">
					<!-- Colonne gauche : Images -->
					<div class="single__watches-gallery">
						<!-- Image principale -->
						<div class="single__watches-images-main">
							{% if post.images is not empty %}
								<a href="{{ Image(post.images[0]).src }}" data-lightbox="watches" id="main-watch-image">
									<img
										src="{{ Image(post.images[0]).src }}"
										srcset="{{ Image(post.images[0]).src }}"
										alt="{{ Image(post.images[0]).alt }}"
										class="main-image"
									/>
								</a>
							{% else %}
								<a href="{{ Image(post.thumbnail).src }}" data-lightbox="watch">
									<img
										src="{{ Image(post.thumbnail).src }}"
										srcset="{{ Image(post.thumbnail).src }}"
										alt="{{ Image(post.thumbnail).alt }}"
										class="main-image"
									/>
								</a>
							{% endif %}
						</div>

						<!-- Vignettes -->
						{% if post.images is not empty and post.images|length > 1 %}
							<div class="single__watches-thumbnails">
								{% for key, galerie in post.images %}
									<div class="thumbnail-item {% if key == 0 %}active{% endif %}" data-image-index="{{ key }}">
										<img 
											src="{{ Image(galerie).src }}"
											alt="{{ Image(galerie).alt }}"
											data-full-image="{{ Image(galerie).src }}"
										/>
									</div>
								{% endfor %}
							</div>
						{% endif %}
					</div>

					<!-- Colonne droite : Informations -->
					<div class="single__watches__info">
						<!-- Titre -->
						<h1 class="single__watches__title">
							{{ post.title }}
						</h1>

						<!-- Prix -->
						{% if post.custom_fields.price %}
							<div class="single__watches__price">
								€{{ post.custom_fields.price }}
							</div>
						{% endif %}

						<!-- Description principale -->
						<div class="single__watches__description">
							{{ post.content }}
						</div>

						<!-- Champs ACF supplémentaires -->
						<?php if (get_field('long_title') || get_field('long')): ?>
							<div class="single__watches__acf-section">
								<?php if (get_field('long_title')): ?>
									<h2 class="title_h2_acf"><?= esc_html(get_field('long_title')); ?></h2>
								<?php endif; ?>
								<?php if (get_field('long')): ?>
									<p><?= wp_kses_post(get_field('long')); ?></p>
								<?php endif; ?>
							</div>
						<?php endif; ?>

						<?php if (get_field('short_title') || get_field('short')): ?>
							<div class="single__watches__acf-section">
								<?php if (get_field('short_title')): ?>
									<h2 class="title_h2_acf"><?= esc_html(get_field('short_title')); ?></h2>
								<?php endif; ?>
								<?php if (get_field('short')): ?>
									<p><?= wp_kses_post(get_field('short')); ?></p>
								<?php endif; ?>
							</div>
						<?php endif; ?>

						<!-- Caractéristiques techniques -->
						<div class="single__watches__specs">
							{% if post.custom_fields.diameter %}
								<p><strong>Diamètre :</strong> {{ post.custom_fields.diameter }}</p>
							{% endif %}
							{% if post.custom_fields.availability %}
								<p><strong>Disponible au</strong> <a href="{{ post.custom_fields.availability_link }}">{{ post.custom_fields.availability }}</a></p>
							{% endif %}
							{% if post.custom_fields.reference %}
								<p><strong>Réf:</strong> {{ post.custom_fields.reference }}</p>
							{% endif %}
						</div>

						<!-- Garantie -->
						<div class="single__watches__warranty">
							<h2 class="title_h2_acf">Garantie & Services</h2>
							<ul>
								<li>Montre révisée et garantie 3 ans par notre atelier.</li>
							</ul>
						</div>
					</div>
				</div>

				<!-- Same Spirit Section -->
				{% include 'partial/same-spirit.twig' with { post: post } only %}
			</article>
		</div>

		<!-- ===== MOBILE ===== -->
		<div class="content-wrapper content-wrapper-mobile">
			<article class="post-type-{{ post.post_type }} single__watches single__watches--new-layout mobile" id="post-{{ post.ID }}">
				<div class="single__watches--container">
					<!-- Image principale mobile -->
					<div class="single__watches-images">
						<div class="mobile-carousel">
							<div class="mobile-carousel__track">
								{% if post.images is not empty %}
									{% for key, galerie in post.images %}
										<div class="mobile-carousel__slide">
											<a href="{{ Image(galerie).src }}" data-lightbox="watches-mobile">
												<img
													src="{{ Image(galerie).src }}"
													alt="{{ Image(galerie).alt }}"
													loading="lazy"
												/>
											</a>
										</div>
									{% endfor %}
								{% else %}
									<div class="mobile-carousel__slide">
										<a href="{{ Image(post.thumbnail).src }}" data-lightbox="watch-mobile">
											<img
												src="{{ Image(post.thumbnail).src }}"
												alt="{{ Image(post.thumbnail).alt }}"
												loading="lazy"
											/>
										</a>
									</div>
								{% endif %}
							</div>
						</div>
					</div>

					<!-- Informations mobile -->
					<div class="single__watches__text-part-wrapper">
						<div class="single__watches__text-part">
							<h1 class="single__watches__title">
								{{ post.title }}
							</h1>

							{% if post.custom_fields.price %}
								<div class="single__watches__price">
									€{{ post.custom_fields.price }}
								</div>
							{% endif %}

							<div class="single__watches__content">
								{{ post.content }}
							</div>

							<div class="single__watches__button-container">
								<a class="cta cta__interest" href="{{ interest.url }}">{{ interest.label }}</a>
								<a class="cta cta__return" href="{{ watches.url }}">{{ watches.label }}</a>
							</div>
						</div>
					</div>
				</div>

				{% include 'partial/same-spirit.twig' with { post: post } only %}
			</article>
		</div>
	{% endfor %}
{% endblock %}

<style>
/**
 * SINGLE WATCH - NOUVELLE MISE EN PAGE
 * Styles CSS pour la présentation des montres
 * Version: 1.0.0
 */

/* ========================================
   VARIABLES CSS (Personnalisables)
   ======================================== */
:root {
  --primary-color: #333;
  --secondary-color: #666;
  --accent-color: #4CAF50;
  --border-color: #e0e0e0;
  --bg-light: #f9f9f9;
  --bg-hover: #e9e9e9;
  --text-dark: #333;
  --text-light: #666;
  --white: #fff;
  --black: #000;
}

/* ========================================
   CONTENEUR PRINCIPAL
   ======================================== */
.single__watches--new-layout .single__watches--container {
  display: flex;
  gap: 60px;
  max-width: 1400px;
  margin: 0 auto;
  padding: 40px 20px;
}

@media (max-width: 1024px) {
  .single__watches--new-layout .single__watches--container {
    flex-direction: column;
    gap: 30px;
  }
}

/* ========================================
   GALERIE D'IMAGES (GAUCHE)
   ======================================== */
.single__watches--new-layout .single__watches-gallery {
  flex: 0 0 45%;
  max-width: 600px;
}

@media (max-width: 1024px) {
  .single__watches--new-layout .single__watches-gallery {
    flex: 1;
    max-width: 100%;
  }
}

/* Image principale */
.single__watches--new-layout .single__watches-images-main {
  margin-bottom: 20px;
}

.single__watches--new-layout .single__watches-images-main a {
  display: block;
  cursor: zoom-in;
}

.single__watches--new-layout .single__watches-images-main .main-image {
  width: 100%;
  height: auto;
  object-fit: cover;
  border-radius: 8px;
  transition: transform 0.3s ease;
}

.single__watches--new-layout .single__watches-images-main .main-image:hover {
  transform: scale(1.02);
}

/* Vignettes */
.single__watches--new-layout .single__watches-thumbnails {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.single__watches--new-layout .thumbnail-item {
  flex: 0 0 calc(16.666% - 10px);
  max-width: 100px;
  cursor: pointer;
  border: 2px solid transparent;
  border-radius: 4px;
  overflow: hidden;
  transition: all 0.3s ease;
}

@media (max-width: 768px) {
  .single__watches--new-layout .thumbnail-item {
    flex: 0 0 calc(20% - 10px);
  }
}

.single__watches--new-layout .thumbnail-item.active {
  border-color: var(--primary-color);
}

.single__watches--new-layout .thumbnail-item:hover {
  border-color: var(--secondary-color);
}

.single__watches--new-layout .thumbnail-item img {
  width: 100%;
  height: auto;
  display: block;
}

/* ========================================
   INFORMATIONS PRODUIT (DROITE)
   ======================================== */
.single__watches--new-layout .single__watches__info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

/* Titre */
.single__watches--new-layout .single__watches__title {
  font-size: 28px;
  font-weight: 400;
  letter-spacing: 0.5px;
  margin: 0;
  color: var(--text-dark);
  text-transform: uppercase;
}

@media (max-width: 768px) {
  .single__watches--new-layout .single__watches__title {
    font-size: 24px;
  }
}

/* Prix */
.single__watches--new-layout .single__watches__price {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-dark);
  margin: 10px 0;
}

/* Description */
.single__watches--new-layout .single__watches__description {
  font-size: 14px;
  line-height: 1.8;
  color: var(--text-light);
}

.single__watches--new-layout .single__watches__description p {
  margin-bottom: 15px;
}

/* Sections ACF */
.single__watches--new-layout .single__watches__acf-section {
  margin-top: 20px;
}

.single__watches--new-layout .single__watches__acf-section .title_h2_acf {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 10px;
  color: var(--text-dark);
}

.single__watches--new-layout .single__watches__acf-section p {
  font-size: 14px;
  line-height: 1.8;
  color: var(--text-light);
}

/* Caractéristiques techniques */
.single__watches--new-layout .single__watches__specs {
  margin-top: 20px;
  padding: 20px;
  background-color: var(--bg-light);
  border-radius: 4px;
}

.single__watches--new-layout .single__watches__specs p {
  margin: 8px 0;
  font-size: 14px;
  color: var(--text-light);
}

.single__watches--new-layout .single__watches__specs p strong {
  color: var(--text-dark);
  font-weight: 600;
}

.single__watches--new-layout .single__watches__specs p a {
  color: var(--text-dark);
  text-decoration: underline;
}

.single__watches--new-layout .single__watches__specs p a:hover {
  color: var(--text-light);
}

/* ========================================
   PARTAGE SOCIAL
   ======================================== */
.single__watches--new-layout .single__watches__share {
  display: flex;
  align-items: center;
  gap: 15px;
  margin-top: 20px;
}

.single__watches--new-layout .single__watches__share span {
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 1px;
  color: var(--text-dark);
}

.single__watches--new-layout .single__watches__share .share-icons {
  display: flex;
  gap: 10px;
}

.single__watches--new-layout .share-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background-color: #f0f0f0;
  transition: all 0.3s ease;
}

.single__watches--new-layout .share-icon svg {
  width: 16px;
  height: 16px;
  fill: var(--text-dark);
}

.single__watches--new-layout .share-icon:hover {
  background-color: var(--primary-color);
}

.single__watches--new-layout .share-icon:hover svg {
  fill: var(--white);
}

/* ========================================
   ACTIONS (QUANTITÉ + PANIER)
   ======================================== */
.single__watches--new-layout .single__watches__actions {
  display: flex;
  gap: 15px;
  margin-top: 30px;
}

@media (max-width: 768px) {
  .single__watches--new-layout .single__watches__actions {
    flex-direction: column;
  }
}

/* Sélecteur de quantité */
.single__watches--new-layout .quantity-selector {
  display: flex;
  align-items: center;
  border: 1px solid #ddd;
  border-radius: 4px;
  overflow: hidden;
}

.single__watches--new-layout .quantity-btn {
  width: 44px;
  height: 44px;
  border: none;
  background-color: var(--bg-light);
  cursor: pointer;
  font-size: 18px;
  color: var(--text-dark);
  transition: background-color 0.3s ease;
}

.single__watches--new-layout .quantity-btn:hover {
  background-color: var(--bg-hover);
}

.single__watches--new-layout .quantity-btn:active {
  background-color: #ddd;
}

.single__watches--new-layout .quantity-input {
  width: 60px;
  height: 44px;
  border: none;
  text-align: center;
  font-size: 14px;
  font-weight: 600;
  color: var(--text-dark);
}

/* Supprimer les flèches du input number */
.single__watches--new-layout .quantity-input::-webkit-outer-spin-button,
.single__watches--new-layout .quantity-input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  margin: 0;
}

.single__watches--new-layout .quantity-input {
  -moz-appearance: textfield;
}

.single__watches--new-layout .quantity-input {
  transition: transform 0.15s ease;
}

/* Bouton ajouter au panier */
.single__watches--new-layout .cta__add-to-cart {
  flex: 1;
  height: 44px;
  background-color: var(--primary-color);
  color: var(--white);
  border: none;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.single__watches--new-layout .cta__add-to-cart:hover {
  background-color: var(--black);
}

.single__watches--new-layout .cta__add-to-cart:active {
  transform: scale(0.98);
}

/* ========================================
   BOUTONS SECONDAIRES
   ======================================== */
.single__watches--new-layout .single__watches__button-container {
  display: flex;
  gap: 15px;
  margin-top: 20px;
}

@media (max-width: 768px) {
  .single__watches--new-layout .single__watches__button-container {
    flex-direction: column;
  }
}

.single__watches--new-layout .single__watches__button-container .cta {
  flex: 1;
  padding: 12px 24px;
  text-align: center;
  text-decoration: none;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 0.5px;
  transition: all 0.3s ease;
  display: inline-block;
}

.single__watches--new-layout .cta__interest {
  background-color: transparent;
  color: var(--primary-color);
  border: 2px solid var(--primary-color);
}

.single__watches--new-layout .cta__interest:hover {
  background-color: var(--primary-color);
  color: var(--white);
}

.single__watches--new-layout .cta__return {
  background-color: #f0f0f0;
  color: var(--primary-color);
  border: 2px solid #f0f0f0;
}

.single__watches--new-layout .cta__return:hover {
  background-color: #e0e0e0;
  border-color: #e0e0e0;
}

/* ========================================
   GARANTIE
   ======================================== */
.single__watches--new-layout .single__watches__warranty {
  margin-top: 30px;
  padding-top: 30px;
  border-top: 1px solid var(--border-color);
}

.single__watches--new-layout .single__watches__warranty .title_h2_acf {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 15px;
  color: var(--text-dark);
}

.single__watches--new-layout .single__watches__warranty ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.single__watches--new-layout .single__watches__warranty li {
  padding: 10px 0;
  font-size: 14px;
  color: var(--text-light);
  position: relative;
  padding-left: 25px;
}

.single__watches--new-layout .single__watches__warranty li:before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--accent-color);
  font-weight: bold;
}

/* ========================================
   MONTRES SIMILAIRES
   ======================================== */
.single__watches--new-layout .single__watches__similar {
  margin-top: 60px;
  padding-top: 40px;
  border-top: 1px solid var(--border-color);
}

.single__watches--new-layout .single__watches__similar h2 {
  font-size: 24px;
  font-weight: 600;
  margin-bottom: 30px;
  text-align: center;
  color: var(--text-dark);
}

.single__watches--new-layout .similar-watches-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
}

@media (max-width: 1024px) {
  .single__watches--new-layout .similar-watches-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 768px) {
  .single__watches--new-layout .similar-watches-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

.single__watches--new-layout .similar-watch-item {
  text-align: center;
}

.single__watches--new-layout .similar-watch-item a {
  text-decoration: none;
  color: var(--text-dark);
  display: block;
  transition: transform 0.3s ease;
}

.single__watches--new-layout .similar-watch-item a:hover {
  transform: translateY(-5px);
}

.single__watches--new-layout .similar-watch-item img {
  width: 100%;
  height: auto;
  border-radius: 8px;
  margin-bottom: 10px;
}

.single__watches--new-layout .similar-watch-item h3 {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 5px;
}

.single__watches--new-layout .similar-watch-item .price {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-light);
}

/* ========================================
   VERSION MOBILE
   ======================================== */
.content-wrapper-mobile {
  display: none;
}

@media (max-width: 768px) {
  .content-wrapper-mobile {
    display: block;
  }
}

.content-wrapper-mobile .single__watches--new-layout .single__watches--container {
  padding: 20px;
}

/* Carousel mobile */
.content-wrapper-mobile .mobile-carousel {
  position: relative;
  overflow: hidden;
  border-radius: 8px;
}

.content-wrapper-mobile .mobile-carousel__track {
  display: flex;
  scroll-snap-type: x mandatory;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
  cursor: grab;
}

.content-wrapper-mobile .mobile-carousel__track::-webkit-scrollbar {
  display: none;
}

.content-wrapper-mobile .mobile-carousel__track:active {
  cursor: grabbing;
}

.content-wrapper-mobile .mobile-carousel__slide {
  flex: 0 0 100%;
  scroll-snap-align: center;
}

.content-wrapper-mobile .mobile-carousel__slide img {
  width: 100%;
  height: auto;
  display: block;
}

.content-wrapper-desktop {
  display: block;
}

@media (max-width: 768px) {
  .content-wrapper-desktop {
    display: none;
  }
}

/* ========================================
   LIGHTBOX COMPATIBILITY
   ======================================== */
.single__watches--new-layout a[data-lightbox] {
  cursor: zoom-in;
}

/* ========================================
   ANIMATIONS
   ======================================== */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes slideUp {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.single__watches--new-layout .single__watches-gallery {
  animation: fadeIn 0.5s ease-in-out;
}

.single__watches--new-layout .single__watches__info {
  animation: slideUp 0.6s ease-in-out;
}

/* ========================================
   ÉTATS DE CHARGEMENT
   ======================================== */
.single__watches--new-layout .cta__add-to-cart.adding {
  opacity: 0.7;
  pointer-events: none;
}

/* ========================================
   ACCESSIBILITÉ
   ======================================== */
.single__watches--new-layout button:focus,
.single__watches--new-layout a:focus,
.single__watches--new-layout input:focus {
  outline: 2px solid var(--accent-color);
  outline-offset: 2px;
}

.single__watches--new-layout .thumbnail-item:focus {
  border-color: var(--accent-color);
}

/* ========================================
   PRINT STYLES
   ======================================== */
@media print {
  .single__watches--new-layout .single__watches__actions,
  .single__watches--new-layout .single__watches__button-container,
  .single__watches--new-layout .single__watches__share {
    display: none;
  }
  
  .single__watches--new-layout .single__watches--container {
    flex-direction: column;
  }
}

/* ========================================
   UTILITAIRES
   ======================================== */
.single__watches--new-layout .hidden {
  display: none;
}

.single__watches--new-layout .sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

/* ========================================
   FIN DU FICHIER
   ======================================== */
</style>
